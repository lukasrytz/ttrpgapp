import type { ServerPlugin } from '@ttrpgapp/shared/plugin-server';

export const dnd5eServerPlugin: ServerPlugin = {
  id: 'dnd5e',
  name: 'D&D 5e',
  // packs and routes are added in the compendium / combat tracker milestones
};
